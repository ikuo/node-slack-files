{
  "name": "slack-files",
  "description": "A client of Slack Web API on file upload",
  "main": "lib/index.js",
  "version": "0.0.0-semantic-release",
  "scripts": {
    "test": "(mocha && rm -rf ./target && mkdir target && cd target && npm install ../ --save)",
    "install": "coffee -bco lib/ src/",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ikuo/node-slack-files.git"
  },
  "keywords": [
    "csv",
    "map",
    "parser"
  ],
  "author": "Ikuo Matsumura <makiczar@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.3.0",
    "chai-as-promised": "^5.1.0",
    "coffee-script": "^1.10.0",
    "mocha": "^2.3.3",
    "nock": "^2.15.0",
    "semantic-release": "^4.3.5"
  },
  "dependencies": {
    "bluebird": "^2.10.1",
    "lodash": "^3.10.1",
    "request": "^2.64.0"
  }
}
